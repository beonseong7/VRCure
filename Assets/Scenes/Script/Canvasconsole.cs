using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.XR;
using UnityEngine.UI;

public class Canvasconsole : MonoBehaviour
{

    [SerializeField] GameObject Equip_obj;
    bool selecting = false;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
       // var tmp = Equip_obj.GetComponent<bag>().hit;
       //if(tmp.point!=new Vector3(0,0,0))
            //this.GetComponent<Text>().text = Equip_obj.GetComponent<bag>().hit.point.ToString();
        
    }

}
